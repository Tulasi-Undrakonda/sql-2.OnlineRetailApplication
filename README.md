Task 2: Data Insertion and Handling NULLs

This project includes SQL scripts demonstrating:Insert, Update, Delete

✔INSERT
Adding 30 rows to each table
Using NULL for optional or missing data
Using default values (e.g., timestamps, ENUM defaults)

✔ UPDATE
Examples such as:
UPDATE customers
SET phone = '9998887776'
WHERE customer_id = 2;

✔ DELETE
Examples such as:
DELETE FROM reviews
WHERE review_id = 30;
